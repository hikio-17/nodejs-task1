const getName = (name) => name;
const getBirth = (birth) => birth;
const getAddress = (address) => address;
const getPlaceBirth = (placeBirth) => placeBirth;

module.exports = {
  getName,
  getBirth,
  getAddress,
  getPlaceBirth,
};
